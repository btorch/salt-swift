{{ '# This was generated by salt/pillar ' }}
{% for section in pillar['ring-minion-config'] %}
{{ '[%s]' % section }}
{% for key, value in pillar['ring-minion-config'][section].iteritems() -%}
{{ '%s = %s \n' % (key, value) }}
{%- endfor %}
{% endfor %}
