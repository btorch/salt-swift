{{ '# This was generated by salt/pillar (updated on: ' }} {{ salt['cmd.run']('date') }} {{ ')' }}
{% for section in pillar['swift-config'] %}
{{ '[%s]' % section }}
{% for key, value in pillar['swift-config'][section].iteritems() -%}
{{ '%s = %s \n' % (key, value) }}
{%- endfor %}
{% endfor %}
