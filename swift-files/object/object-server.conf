{{ '# This was generated by salt/pillar ' }}
{% for section in pillar['object-config'] %}
{{ '[%s]' % section }}
{% for key, value in pillar['object-config'][section].iteritems() -%}
{{ '%s = %s \n' % (key, value) }}
{%- endfor %}
{% endfor %}
