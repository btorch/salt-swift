{{ '# This was generated by salt/pillar ' }}
{% for section in pillar['proxy-config'] %}
{{ '[%s]' % section }}
{% for key, value in pillar['proxy-config'][section].iteritems() -%}
{{ '%s = %s \n' % (key, value) }}
{%- endfor %}
{% endfor %}
