{{ '# This was generated by salt/pillar ' }}
{% for section in pillar['ring-master-config'] %}
{{ '[%s]' % section }}
{% for key, value in pillar['ring-master-config'][section].iteritems() -%}
{{ '%s = %s \n' % (key, value) }}
{%- endfor %}
{% endfor %}
